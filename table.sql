CREATE DATABASE guided_inquiry;
USE guided_inquiry;

CREATE TABLE car (
make VARCHAR(50),
model VARCHAR(30),
year INT
);

CREATE TABLE books (
title VARCHAR(255),
publish DATE,
first_name VARCHAR(255),
last_name VARCHAR(255)
);

CREATE TABLE movies (
title VARCHAR(255),
release_date DATE,
rating INT
);

